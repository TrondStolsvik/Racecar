function getString() {
  //Declare a variable palindromeInput and store the input from the dom
  //Call palindromeCheck and pass it the variable palindromeInput and store it's data in a new variable called palindromeChecked
  //Call palindromeDisplay and pass it the variable palindromeChecked to display the data in the dom
}

function palindromeCheck(palInput) {
  //Store the data from the parameter palInput in a variable called palindromeInput
  //Call stringReverse and pass it the parameter palindromeInput, store this in a variable called stringReversed
  //For-loop using the data in palindromeInput
  //Use an if-statement to check each index against the same index of stringReversed
  //If they all match, return stringReversed
  //Else return palindromeInput
}

function stringReverse(palindromeInput) {
  //For-loop through the string to reverse it
  //Push the indexes from the string into a new variable called revString, starting from the end of the string
  //Return revString
}

function palindromeDisplay(palindromeChecked) {
  //
}
